# UniNet Connect — Dependencias backend
# Versiones fijadas para reproducibilidad. Subir con `pip-compile` en el futuro.

# --- Framework web ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Validación y configuración ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Base de datos ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# --- Seguridad (auth, hashing, JWT) ---
argon2-cffi==23.1.0
pyjwt==2.10.1
pyotp==2.9.0
cryptography==44.0.0

# --- Cache / colas (Sprint 3+) ---
redis==5.2.1

# --- Utilidades ---
python-dotenv==1.0.1
httpx==0.28.1
